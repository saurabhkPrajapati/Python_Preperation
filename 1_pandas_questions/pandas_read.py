# a = [[30,], [21,]]
# [['Geeks'], ['for',]]

# pd.Series(['4 cups', '1 cup', '2 large', '1 can'], index=['Flour', 'Milk', 'Eggs', 'Spam'], name='Dinner')
# pd.read_csv('../input/wine-reviews/winemag-data_first150k.csv', index_col=0, names=column_names)
# column_names = ['column1', 'column2', 'column3']

# reviews.loc[:9, "description"] it is including limits

# reviews.description.map(lambda X: 'tropical' in X).sum()
